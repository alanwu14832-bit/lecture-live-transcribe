# 開發 Prompt v2（已依需求定案）

> v1 草稿假設要接付費 ASR 與 LLM；v2 依「免費、個人用、教室電腦收音」三個條件重寫。
> 這份就是交給 AI 開發時要貼的完整需求，不再有待填的【】。

## 1. 角色與目標
你是一位資深前端工程師。請為我建立一個**個人用的課堂即時轉錄網頁**：
上課時在教室電腦的 Chrome 開這個網頁，即時把教授講的話轉成文字，並可切換顯示成
**原文／繁體中文／英文／中英對照**。**全程零費用、不需要任何 API 金鑰、不需要後端。**

## 2. 使用者與場景
- 使用者只有我一個人，商學院學生。
- 場景：教室電腦（Windows 或 macOS）的 Chrome 瀏覽器，用電腦內建或外接麥克風收音，
  一堂課 50–180 分鐘，距離講者 3–15 公尺，有空調與同學交談的背景噪音。
- 內容：中文為主，有時中英夾雜；大量財金／管理／會計專有名詞
  （NPV、IRR、WACC、EBITDA、DCF、Porter 五力、SWOT…），教授常把英文術語混在中文句子裡說。

## 3. MVP 功能（第一版必做）
1. **一鍵開始／停止**轉錄，大字逐句顯示，尚未定稿的句子用灰字即時預覽，自動捲到最新。
2. **輸出模式切換**：原文／繁體中文／英文／中英對照，切換時歷史內容也一併轉換。
3. **中英夾雜處理**：辨識語言預設 zh-TW（Chrome 的中文模型能吃常見英文詞），
   另提供 en-US 切換給全英文授課；術語靠校正表保住原詞（見第 4 點）。
4. **術語校正表**：可編輯的「聽錯 → 正確」對照表，辨識定稿後自動替換
   （例：「安批威」→「NPV」、「一比達」→「EBITDA」）；內建一份財金常用表，可增刪。
5. **時間戳與分段**：每段附開始時間，停頓超過 2.5 秒或句尾標點自動分段。
6. **本地儲存**：每場自動存 localStorage，重整不掉；可開歷史場次、改名、刪除。
7. **匯出**：整場逐字稿匯出 Markdown／TXT（含時間戳），一鍵複製。
8. **課後摘要**：Chrome 內建 Summarizer 可用時直接產生重點摘要；不可用時提供
   「複製逐字稿＋摘要提示詞」按鈕，貼到任何免費的 AI 網頁即可。

### 之後再做（第一版不實作）
講者分離、跨裝置同步、多人共享、投影片 OCR、PWA 離線快取。

## 4. 技術約束（免費路線）
- **前端**：純 HTML／CSS／JavaScript，不用框架、不用打包工具、不用 npm。
- **語音辨識**：瀏覽器內建 **Web Speech API**（`SpeechRecognition`），
  `continuous = true`、`interimResults = true`。Chrome 會在靜音或約一分鐘後自動停止，
  必須在 `onend` 自動重啟，並確保重啟間不掉字。
- **翻譯**：Chrome 內建 **Translator API**（Chrome 138+，模型在本機跑，免費），
  搭配 **LanguageDetector API** 判斷每段是中翻英還是英翻中。
  不支援的瀏覽器要明確提示，並退化成只有「原文」模式，其他功能照常。
- **摘要**：Chrome 內建 **Summarizer API** 可用才用；否則走「複製提示詞」退路。
- **儲存**：localStorage，key 前綴 `llt:`；資料結構要有版本號方便日後遷移。
- **部署**：GitHub Pages（免費、自帶 HTTPS；麥克風與內建 AI 都要求安全來源）。
  不需要 Vercel、不需要環境變數。
- **不做的事**：不呼叫任何付費或需金鑰的 API；不把音訊存檔；不加後端。

## 5. 非功能需求
- 延遲：講完到畫面出現定稿原文 ≤ 2 秒；譯文 ≤ 3 秒（本機模型）。
- 穩定：辨識自動重啟不可漏字；分頁切到背景時要顯示醒目警告（Chrome 可能暫停麥克風）。
- 第一次使用：清楚引導「允許麥克風」與「等待翻譯模型下載」兩個步驟，並顯示下載進度。
- 可讀性：字級可調（三段）、深色模式、桌面優先版面（左轉錄區、右側術語表與設定）。
- 隱私提示：頁面要明白告知「Web Speech API 會把音訊送到 Google 伺服器辨識」。

## 6. 交付方式與驗收
- 先給架構說明與目錄結構，確認後再寫程式（見 `docs/ARCHITECTURE.md`）。
- 程式碼要有繁體中文註解，說明「為什麼這樣做」而不只是「做了什麼」。
- README：如何用 GitHub Pages 部署、第一次開啟要做什麼、哪些瀏覽器支援。
- 驗收（在 Chrome 手動測）：
  1. 對著麥克風唸一段含 NPV、WACC 的中文句子，原文正確、術語保留英文。
  2. 切到「中英對照」，歷史段落全部出現譯文。
  3. 靜音 90 秒後再講話，仍能繼續辨識、沒有漏字。
  4. 重新整理頁面，逐字稿還在。
  5. 匯出 Markdown 可以正常打開，有時間戳。

## 7. 回答風格
繁體中文；先講結論再講細節；每個技術選擇說明取捨與替代方案；不確定的地方直接列出來問，不要自己猜重要決策。
